  version: '3'

  includes:
    redpanda-benthos:
      taskfile: ./redpanda-benthos/Taskfile.yml
      dir: ../
    redpanda-connect:
      taskfile: ./redpanda-connect/Taskfile.yml
      dir: ../

  tasks:
    build:
      desc: "Build the plugins"
      cmds:
        - task: redpanda-benthos:build
        - task: redpanda-connect:build
